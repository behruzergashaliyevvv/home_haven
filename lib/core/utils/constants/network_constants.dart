class NetworkConstants {
  // base url
  static String baseUrl = "https://e-commerce.birnima.uz";

  // auth url
  static String authUrl = "$baseUrl/api/auth";

  // banners url
  static String bannerUrl = "$baseUrl/api/banner";
  static String productUrl = "$baseUrl/api/product";
}
